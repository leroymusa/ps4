**Compile the Program**:
   ```bash
   gcc -o ps4 ps4.c
   ```

**Running the Program**:

**Run with Root Permissions**: To access the input device, run the program with `sudo`:
   ```bash
   sudo ./ps4